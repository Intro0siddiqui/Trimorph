name: Trimorph CI

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Run bats tests
        run: |
          ./test/bats-core/bin/bats test/core.bats